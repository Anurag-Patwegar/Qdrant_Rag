# Qdrant_Rag
This project builds a fully local Retrieval-Augmented Generation (RAG) system using TinyLlama, Qdrant, and sentence-transformers inside GitHub Codespaces. It runs an OpenAI-compatible LLM server, retrieves relevant documents via vector search, and generates contextual answers—replicating a mini ChatGPT-style AI stack end-to-end.
